# Mission 145：三款产品内容总结真实试跑

## 结果

本轮使用 M144 最新代码，对 596、5003、1826 三款产品的 9 份冻结 PDF 运行
百炼 `qwen-plus`，共 6 次调用，产物独立保存为
`provider-run-m144-three-products.json`，没有覆盖 M143。

| 产品 | 材料支持字段 | 已抽取 | 材料支持条件下抽取率 | 产品简介 | 产品概览 |
|---|---:|---:|---:|---|---|
| 596 | 28 | 26 | 92.86% | present | present |
| 5003 | 24 | 19 | 79.17% | present | present |
| 1826 | 25 | 21 | 84.00% | present | present |
| **合计** | **77** | **66** | **85.71%** | **3/3** | **3/3** |

抽取率按“抽取出来的字段 / 当前材料能支持的字段”计算；材料明确不足或无法
判定的字段不进入分母。

## 与 M143 对比

M143 同三款产品的基线为 `65/77=84.42%`，本轮为 `66/77=85.71%`，加权提升
`1.30` 个百分点。分产品变化为：596 `0` 个百分点，5003 `-4.17` 个百分点，
1826 `+8.00` 个百分点。说明内容总结补抽已生效，但事实字段仍有模型输出方差，
不能只看总数判断所有字段都变好。

两个新目标字段在三款产品均有值。Evidence 总量/严格 `VERIFIED` 数量分别为：

| 产品 | 产品简介 | 产品概览 |
|---|---|---|
| 596 | 11 / 8 | 13 / 11 |
| 5003 | 5 / 0 | 3 / 0 |
| 1826 | 4 / 1 | 5 / 1 |

`NORMALIZED_MATCH` 表示经过归一化后匹配，仍应在后续审核中补做语义确认，不能
直接当作严格页码证据通过。

## 调用与服务回执

- run：`v5-trial-38e10677cadec24d`；`COMPLETED`；6/6 次调用。
- 596 的第二次补抽返回 `finish_reason=length`，以
  `LLM_RESULT_NOT_COMPLETE` 拒绝，首轮结果已保留。
- 8091 和 5174 均已指向本文件，实时接口返回上述 run 和 3 个产品。
- provisioning、GitHub live 未执行；没有生产 DB、Candidate、Release、Active
  或 GitHub 写入。

## 下一步

先把内容总结拆成“简介”和“概览”两个更短的定向任务，限制每次输出长度，避免
长上下文触发截断；事实字段继续保留首轮结果，并针对回退字段做定向证据复核。
完成重复运行的一致性评测后，再决定是否增加调用预算。
