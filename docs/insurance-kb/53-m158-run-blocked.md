# M158 运行回执

## 当前结论

Mission 158 的软件实现已完成，真实六款产品运行未形成可用结果文件。运行在 31 次百炼调用后进入本地合同封装，发现继承的 `V5ProviderTrialRun` 总调用上限仍为 30，导致封装失败；已将该合同上限修正为批准的 32，并通过回归测试。

本轮没有写入 M158 result、audit 或 feedback 文件，也没有更新 8091/5174。M157 结果保持不变。

## 已消耗调用

- 主调用：29 次。
- 重试：2 次，均为 1828-1 的长字段，返回 `LLM_RESULT_NOT_COMPLETE`，半截内容未入字段。
- 成功返回的主调用：27 次；1828-1 的最后短字段批次成功返回，但整体因封装门槛未落盘。
- 429：0 次。

## 软件验证

- V5 回归：`80 passed`。
- Ruff：通过。
- 严格 mypy：通过。
- compileall：通过。

## 下一步

需要新的调用预算授权后，直接使用已修正合同重跑六款。疾病定义与责任免除应继续缩小为章节/疾病组分片，避免 2 万字符级单片再次出现十分钟级等待或未结束响应。
